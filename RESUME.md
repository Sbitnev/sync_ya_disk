# 📊 Сводка по синхронизации и конвертации файлов

## ✅ Что скачивается и конвертируется

### Документы (конвертируются в Markdown/CSV)

| Формат | Расширения | Результат |
|--------|-----------|-----------|
| **Word** | .docx, .doc | → Markdown |
| **Excel** | .xlsx, .xls, .xlsm, .xlsb | → CSV (каждый лист отдельно) |
| **CSV** | .csv | → Markdown (с поддержкой UTF-16) |
| **PowerPoint** | .pptx, .ppt | → Markdown (через LibreOffice) |
| **PDF** | .pdf | → Markdown (до 100 страниц) |
| **RTF** | .rtf | → Markdown |
| **HTML** | .html, .htm | → Markdown |

### Текстовые файлы (конвертируются в Markdown)

- **Код**: .py, .js, .java, .cpp, .c, .h, .cs, .php, .rb, .go, .rs, .swift, .kt, .scala, .sql
- **Конфиги**: .json, .xml, .yaml, .yml, .ini, .conf, .toml, .properties
- **Текст**: .txt, .md, .rst, .log
- **Шаблоны**: .j2 (Jinja2)
- **Схемы**: .avsc (Avro Schema)

### Архивы (распаковываются и конвертируются)

- **.zip, .7z, .rar, .tar, .gz** → содержимое извлекается и обрабатывается
- Максимальная вложенность: **10 уровней**
- Защищенные паролем архивы пропускаются с предупреждением

---

## ❌ Что **НЕ** скачивается (пропускается)

### Медиа файлы

#### Видео (SKIP_VIDEO_FILES = True)
```
.mp4, .avi, .mov, .mkv, .webm, .flv, .wmv, .m4v,
.mpg, .mpeg, .3gp, .ogv, .vob, .ts
```

#### Изображения (SKIP_IMAGE_FILES = True)
```
.png, .jpg, .jpeg, .gif, .bmp, .tif, .tiff,
.svg, .webp, .ico
```

#### Аудио
```
.mp3, .m4a, .ogg
```

### Big Data форматы (SKIP_PARQUET_FILES = True)

- **Parquet**: .parquet
  - Причина: увеличивается в 17-18 раз при конвертации в CSV
  - Рекомендация: работать напрямую через pandas/DuckDB
- **Snappy**: .snappy (сжатые файлы, требуют специальной обработки)

### Временные и служебные файлы

| Паттерн | Описание |
|---------|----------|
| `~$*` | Временные Office файлы (~$filename.docx) |
| `~WRL*.tmp` | Временные Word файлы (~WRL1234.tmp) |
| `*.tmp`, `*.temp`, `*.dtmp` | Все временные файлы |
| `*.drawio`, `*.drawio.bkp`, `*.drawio.dtmp` | Диаграммы Draw.io |
| `._*` | macOS временные файлы (._filename) |
| `.~*` | Скрытые временные файлы (.~lock) |
| `_SUCCESS` | Hadoop/Spark служебные файлы (маркеры успеха) |
| `/^[0-9A-F]{8}$/` | Файлы без расширения с hex-именами (8 символов) |

### Неподдерживаемые форматы

| Расширение | Формат | Причина |
|-----------|--------|---------|
| .mpp | Microsoft Project | Не поддерживается |
| .pbix | Power BI | Не поддерживается |
| .epf, .erf | 1С обработки/отчеты | Специфический формат 1С |
| .sel | Файлы запросов | Специфический формат |
| .mht | MHTML веб-архивы | Не поддерживается |
| .lnk | Ярлыки Windows | Не содержат данных |
| .rdp | Удаленный рабочий стол | Не содержат данных |

---

## 📏 Ограничения

### Размер файлов

| Тип файла | Максимальный размер | Переменная |
|-----------|-------------------|-----------|
| **Общие файлы** | 100 МБ | `MAX_FILE_SIZE` |
| **Табличные файлы** (CSV, Excel, Parquet) | 3 МБ | `MAX_TABULAR_FILE_SIZE` |
| **Общий лимит загрузки** | 100 ГБ | `MAX_TOTAL_SIZE` |
| **Видео для транскрибации** | 500 МБ | `VIDEO_MAX_SIZE` (отключено) |

### Ограничения по содержимому

| Формат | Ограничение |
|--------|-------------|
| **PDF** | Первые 100 страниц (`PDF_MAX_PAGES`) |
| **Excel** | Без ограничений по строкам/столбцам/листам* |
| **CSV** | Без ограничений по строкам/столбцам* |
| **Parquet** | Без ограничений* (но не конвертируется) |
| **Архивы** | Максимальная глубина вложенности: 10 уровней |

\* Можно настроить через `*_MAX_ROWS`, `*_MAX_COLUMNS`, `*_MAX_SHEETS`

---

## 📂 Структура директорий

```
sync_ya_disk/
├── localdata/                      # Все данные (кроме логов)
│   ├── downloaded_files/           # Оригиналы файлов (удаляются после конвертации)
│   ├── markdown_files/             # Конвертированные Markdown файлы
│   ├── metadata/                   # Метаданные синхронизации
│   │   └── sync_metadata.db        # SQLite база данных
│   └── failed_downloads.txt        # Список файлов с ошибками
├── logs/                           # Лог-файлы
└── temp/                           # Тестовые скрипты
```

### Политика удаления оригиналов

**DELETE_ORIGINALS_AFTER_CONVERSION = True**

После успешной конвертации в Markdown оригинальные файлы удаляются для экономии места.

---

## ⚙️ Производительность и настройки

### Параллелизм

| Параметр | Значение | Описание |
|----------|----------|----------|
| **MAX_WORKERS** | 3 | Параллельная загрузка файлов |
| **FOLDER_SCAN_WORKERS** | 3 | Параллельное сканирование папок |
| **HTTP_POOL_CONNECTIONS** | 30 | Размер connection pool |
| **HTTP_POOL_MAXSIZE** | 50 | Максимальный размер пула |

### Надежность

| Параметр | Значение | Описание |
|----------|----------|----------|
| **MAX_RETRIES** | 3 | Количество повторных попыток при ошибках |
| **RETRY_DELAY** | 2 сек | Задержка между попытками |
| **REQUEST_TIMEOUT** | 30 сек | Таймаут для сетевых запросов |
| **DOWNLOAD_CHUNK_SIZE** | 1 МБ | Размер chunk для загрузки |

### Кэширование

| Параметр | Значение | Описание |
|----------|----------|----------|
| **ENABLE_FILES_CACHE** | True | Кэширование списка файлов |
| **FILES_CACHE_LIFETIME** | 300 сек | Время жизни кэша (5 минут) |

---

## 🎯 Итоговая таблица

### ✅ Конвертируется (11 типов)

| № | Тип | Форматы | → Результат |
|---|-----|---------|-------------|
| 1 | Word | .docx, .doc | Markdown |
| 2 | Excel | .xlsx, .xls, .xlsm, .xlsb | CSV |
| 3 | CSV | .csv | Markdown |
| 4 | PowerPoint | .pptx, .ppt | Markdown |
| 5 | PDF | .pdf | Markdown |
| 6 | RTF | .rtf | Markdown |
| 7 | HTML | .html, .htm | Markdown |
| 8 | Текст | .txt, .md, .log | Markdown |
| 9 | Код | .py, .js, .java, .sql и др. | Markdown |
| 10 | Конфиги | .json, .xml, .yaml и др. | Markdown |
| 11 | Архивы | .zip, .7z, .rar, .tar, .gz | Распаковка + конвертация содержимого |

### ❌ Пропускается (5 категорий)

| № | Категория | Примеры | Причина |
|---|-----------|---------|---------|
| 1 | **Медиа** | Видео, изображения, аудио | Не содержат текст для анализа |
| 2 | **Big Data** | Parquet, Snappy | Огромное увеличение размера при конвертации |
| 3 | **Временные** | ~$*, .tmp, .temp, ._* | Служебные файлы |
| 4 | **Неподдерживаемые** | .mpp, .pbix, .epf, .mht | Требуют специальных библиотек |
| 5 | **Слишком большие** | Файлы > 100 МБ (таблицы > 3 МБ) | Ограничение размера |

---

## 📝 Примечания

### Особенности конвертации

1. **Excel → CSV**
   - Каждый лист сохраняется в отдельный CSV файл
   - Формат имени: `файл_лист1_НазваниеЛиста.csv`
   - Пустые листы пропускаются

2. **CSV кодировки**
   - Поддерживается UTF-16 с BOM и без BOM
   - Автоматическое определение кодировки через chardet
   - Fallback на несколько популярных кодировок

3. **Word документы**
   - Изображения заменяются на текстовые заглушки `[Image: filename.png]`
   - Сохраняется структура заголовков и форматирование

4. **Архивы**
   - Рекурсивная распаковка до 10 уровней вложенности
   - Защита от zip-бомб через ограничение глубины
   - Файлы с паролем пропускаются с предупреждением

5. **LibreOffice**
   - Используется для конвертации .doc → .docx и .ppt → .pptx
   - Требует установки Java на Linux серверах
   - На Windows работает из коробки

### Известные проблемы

1. **HTTP 400 ошибки** - файлы со специальными символами в имени (`:`, `(время)`)
2. **Поврежденные файлы** - Excel/Word с некорректным XML не восстанавливаются
3. **Ложные архивы** - файлы с расширением .zip, но не являющиеся ZIP архивами
4. **Пустые документы** - полностью пустые .doc/.docx файлы не конвертируются

---

## 🔧 Настройка

Все параметры настраиваются в `src/config.py`:

```python
# Отключить конвертацию видео
SKIP_VIDEO_FILES = True

# Изменить лимиты размера
MAX_FILE_SIZE = 100 * 1024 * 1024  # 100 МБ
MAX_TABULAR_FILE_SIZE = 3 * 1024 * 1024  # 3 МБ

# Включить конвертацию Parquet (не рекомендуется)
SKIP_PARQUET_FILES = False
CONVERT_PARQUET_FILES = True

# Настроить параллелизм
MAX_WORKERS = 5
FOLDER_SCAN_WORKERS = 5
```

---

**Дата обновления:** 2026-01-26

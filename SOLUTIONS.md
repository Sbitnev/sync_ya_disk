# Решения для автоматической выгрузки всех файлов с Яндекс.Диска

## Проблема
API Яндекс.Диска не возвращает прямые ссылки для файлов в подпапках публичных ссылок, если у файлов нет собственного `public_url`.

## Решения

### 🏆 1. rclone (РЕКОМЕНДУЕТСЯ)

**Преимущества:**
- ✅ Специализированный инструмент для облачных хранилищ
- ✅ Поддержка инкрементальной синхронизации
- ✅ Многопоточность и retry
- ✅ Кроссплатформенность
- ✅ Может работать как демон

**Недостатки:**
- ⚠️ Требует установки отдельной программы
- ⚠️ Для публичных ссылок могут быть ограничения

**Установка:**
```bash
# Windows (Chocolatey)
choco install rclone

# Или скачать: https://rclone.org/downloads/
```

**Использование:**
```bash
# Настройка
rclone config

# Синхронизация
rclone sync yandex:/ ./downloaded_files --progress --transfers=5
```

📄 Подробнее: `setup_rclone.md`

---

### 🌐 2. Selenium (браузерная автоматизация)

**Преимущества:**
- ✅ Обходит ограничения API
- ✅ Работает как настоящий пользователь
- ✅ Гарантированно скачает всё

**Недостатки:**
- ⚠️ Требует Chrome + ChromeDriver
- ⚠️ Медленнее API
- ⚠️ Может сломаться при изменении UI Яндекса
- ⚠️ Требует открытого браузера

**Установка:**
```bash
pip install selenium
# + установить ChromeDriver
```

**Использование:**
```bash
python browser_downloader.py
```

📄 Файл: `browser_downloader.py`

---

### 🔑 3. OAuth + yadisk (полный доступ к своему диску)

**Преимущества:**
- ✅ Официальная библиотека
- ✅ Работает через сохранение на свой диск
- ✅ Прогресс-бары
- ✅ Стабильно

**Недостатки:**
- ⚠️ Требует создания OAuth приложения
- ⚠️ Владелец может запретить "Сохранение на диск"
- ⚠️ Занимает место на вашем Яндекс.Диске

**Настройка:**
1. Создайте приложение: https://oauth.yandex.ru/client/new
2. Права: `cloud_api:disk.read`, `cloud_api:disk.write`
3. Добавьте CLIENT_ID и CLIENT_SECRET в `.env`

**Использование:**
```bash
pip install yadisk
python oauth_downloader.py
```

📄 Файл: `oauth_downloader.py`

---

### 💡 4. Попросить владельца диска

**Самое простое решение:**

1. Попросить владельца создать публичные ссылки на подпапки
2. Или попросить расшарить папку напрямую вам
3. Или попросить разрешить "Сохранение на диск"

---

## Сравнение решений

| Решение   | Сложность | Скорость | Надёжность | Ограничения API |
|-----------|-----------|----------|------------|-----------------|
| rclone    | Средняя   | ★★★★★    | ★★★★☆      | Возможны        |
| Selenium  | Средняя   | ★★☆☆☆    | ★★★☆☆      | Обходит         |
| OAuth     | Высокая   | ★★★★☆    | ★★★★★      | Зависит от прав |
| Попросить | Низкая    | ★★★★★    | ★★★★★      | Решает проблему |

---

## Рекомендация

**Для разового скачивания:** Selenium
**Для регулярной синхронизации:** rclone
**Для программной интеграции:** OAuth
**Для долгосрочного решения:** Попросить владельца настроить доступ

---

## Тестирование

Для проверки, какие файлы можно скачать:

```bash
# Список всех файлов с указанием доступности
python src/list_files.py

# Тест скачивания 10 файлов из папки
python test_download.py "Название папки"
```

---

## Текущая статистика проекта

**Из 7159 файлов:**
- Скачано: 415 (5.8%) - файлы с public_url
- Видео (пропущено): 530 (7.4%)
- Большие файлы (пропущено): 84 (1.2%)
- Без доступа: ~6000+ (83.5%) - нет public_url

**Вывод:** Для полного скачивания нужно обходное решение (rclone, Selenium или OAuth).

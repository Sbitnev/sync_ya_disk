# Синхронизация с Яндекс Диском

Скрипт для автоматической синхронизации файлов с публичной папки Яндекс Диска.

## Возможности

- 🚀 **Многопоточная загрузка** - до 5 файлов одновременно для максимальной скорости
- 🔄 **Автоматические повторы** - автоматический retry при сетевых ошибках (до 3 попыток)
- 📊 **Предварительная статистика** - отображение количества файлов и объема перед началом загрузки
- 📈 **Прогресс-бары** - визуальное отслеживание процесса загрузки каждого файла и общего прогресса
- ♻️ **Инкрементальная синхронизация** - скачиваются только новые или измененные файлы
- 📁 **Сохранение структуры** - полное воспроизведение структуры папок
- 🎬 **Пропуск видео** - автоматическое создание пустых файлов вместо видео
- 📦 **Пропуск больших файлов** - опциональный пропуск файлов >300 МБ
- 🔍 **Отслеживание изменений** - по дате модификации, размеру и MD5
- 📋 **Детальная статистика** - полный отчет по завершению синхронизации

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` в корне проекта со следующим содержимым:
```
Token=ваш_токен_яндекс_диска
```

## Логирование

Скрипт использует библиотеку `loguru` для логирования:

- **Консоль** - отображаются сообщения уровня INFO и выше с цветовой подсветкой
- **Файл** `logs/sync_ya_disk.log` - все сообщения (DEBUG и выше) с ротацией по 10 МБ
- **Хранение** - логи хранятся 7 дней, старые автоматически архивируются в .zip
- **Папка** - все логи находятся в директории `logs/`

Уровни логирования:
- `DEBUG` - детальная отладочная информация
- `INFO` - общая информация о процессе
- `SUCCESS` - успешное завершение операций
- `WARNING` - предупреждения (не критичные проблемы)
- `ERROR` - ошибки, требующие внимания

## Использование

Запустите скрипт:
```bash
python src/main.py
```

При первом запуске будут скачаны все файлы. При повторных запусках будут скачиваться только:
- Новые файлы
- Файлы, которые были изменены (по дате, размеру или MD5)

## Конфигурация

В файле `src/main.py` можно настроить:
- `DOWNLOAD_DIR` - директория для скачивания файлов (по умолчанию: `downloaded_files`)
- `METADATA_FILE` - файл с метаданными (по умолчанию: `sync_metadata.json`)
- `LARGE_FILE_SIZE` - размер файла для пропуска (по умолчанию: 300 МБ)
- `MAX_WORKERS` - количество потоков для загрузки (по умолчанию: 5)
- `MAX_RETRIES` - количество попыток при ошибках (по умолчанию: 3)
- `RETRY_DELAY` - задержка между попытками в секундах (по умолчанию: 2)
- `public_url` - публичная ссылка на Яндекс Диск

### Настройка многопоточности

Для изменения количества одновременно загружаемых файлов:
```python
syncer = YandexDiskSyncer(public_url, skip_large_files=True, max_workers=10)
```

**Рекомендации:**
- 5-10 потоков - оптимально для большинства случаев
- Больше 10 потоков может привести к ограничениям API

## Структура проекта

```
sync_ya_disk/
├── src/
│   └── main.py           # Основной скрипт
├── logs/                 # Логи (создается автоматически)
│   └── sync_ya_disk.log  # Основной лог-файл
├── downloaded_files/     # Скачанные файлы (создается автоматически)
├── sync_metadata.json    # Метаданные о файлах (создается автоматически)
├── .env                  # Токен доступа
├── requirements.txt      # Зависимости
├── README.md            # Документация
└── LOGGING.md           # Документация по логированию
```

## Как работает синхронизация

1. Скрипт получает список всех файлов из публичной папки
2. Для каждого файла проверяется:
   - Существует ли он локально
   - Изменился ли он с момента последней синхронизации
3. Скачиваются только новые или измененные файлы
4. Метаданные сохраняются в `sync_metadata.json` для следующей синхронизации
